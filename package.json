{
  "name": "@valle-design/dwg-conformance",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "check": "node scripts/check-corpus.mjs",
    "test": "node --test tests/check-corpus.spec.mjs",
    "check:all": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
